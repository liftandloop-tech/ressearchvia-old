import { Queue, FlowProducer } from 'bullmq';
import { PrismaService } from '../../prisma.service';
import { RedisService } from '../redis/redis.service';
import { QueueJobStatus } from '@prisma/client';
export declare class QueueService {
    private readonly prisma;
    private readonly redisService;
    private readonly signalQueue;
    private readonly orderPlacementQueue;
    private readonly orderMonitoringQueue;
    private readonly notificationQueue;
    private readonly signalDlq;
    private readonly orderDlq;
    private readonly orderMonitoringDlq;
    private readonly notificationDlq;
    private readonly outboxDispatcherQueue;
    private readonly outboxDispatcherDlq;
    private readonly websocketQueue;
    private readonly websocketDlq;
    private readonly reportGenerationQueue;
    private readonly reportGenerationDlq;
    private readonly reportExportQueue;
    private readonly reportExportDlq;
    private readonly analyticsSnapshotQueue;
    private readonly analyticsSnapshotDlq;
    private readonly positionRebuildQueue;
    private readonly positionRebuildDlq;
    private readonly reconciliationQueue;
    private readonly reconciliationDlq;
    private readonly riskRecalculateQueue;
    private readonly riskRecalculateDlq;
    private readonly analyticsRecalculateQueue;
    private readonly analyticsRecalculateDlq;
    private readonly emailQueue;
    private readonly emailDlq;
    private readonly smsQueue;
    private readonly smsDlq;
    private readonly whatsappQueue;
    private readonly whatsappDlq;
    private readonly pushQueue;
    private readonly pushDlq;
    private readonly logger;
    private readonly flowProducer;
    private readonly shardedSnapshotQueues;
    constructor(prisma: PrismaService, redisService: RedisService, signalQueue: Queue, orderPlacementQueue: Queue, orderMonitoringQueue: Queue, notificationQueue: Queue, signalDlq: Queue, orderDlq: Queue, orderMonitoringDlq: Queue, notificationDlq: Queue, outboxDispatcherQueue: Queue, outboxDispatcherDlq: Queue, websocketQueue: Queue, websocketDlq: Queue, reportGenerationQueue: Queue, reportGenerationDlq: Queue, reportExportQueue: Queue, reportExportDlq: Queue, analyticsSnapshotQueue: Queue, analyticsSnapshotDlq: Queue, positionRebuildQueue: Queue, positionRebuildDlq: Queue, reconciliationQueue: Queue, reconciliationDlq: Queue, riskRecalculateQueue: Queue, riskRecalculateDlq: Queue, analyticsRecalculateQueue: Queue, analyticsRecalculateDlq: Queue, emailQueue: Queue, emailDlq: Queue, smsQueue: Queue, smsDlq: Queue, whatsappQueue: Queue, whatsappDlq: Queue, pushQueue: Queue, pushDlq: Queue);
    getFlowProducer(): FlowProducer;
    getQueue(queueName: string): Queue;
    addJob(queueName: string, jobId: string, payload: any, priority?: number, delay?: number): Promise<void>;
    updateJobStatus(queueName: string, jobId: string, status: QueueJobStatus, attempts?: number): Promise<void>;
    getAggregatedMetrics(): Promise<{
        waiting: number;
        active: number;
        failed: number;
        dlq: number;
    }>;
    getDlqMetrics(): Promise<{
        signalDlq: number;
        orderPlacementDlq: number;
        orderMonitoringDlq: number;
        notificationDlq: number;
        outboxDispatcherDlq: number;
        websocketDlq: number;
        reportGenerationDlq: number;
        reportExportDlq: number;
        positionRebuildDlq: number;
        reconciliationDlq: number;
        riskRecalculateDlq: number;
        analyticsRecalculateDlq: number;
        emailDlq: number;
        smsDlq: number;
        whatsappDlq: number;
        pushDlq: number;
        analyticsSnapshotDlq: number;
    }>;
}
