import axios from 'axios';
import fs from 'fs';

async function testUpload() {
  try {
    // We can just login first
    console.log("Please write a test to hit the endpoint.");
  } catch (err) {
    console.error(err);
  }
}
testUpload();
